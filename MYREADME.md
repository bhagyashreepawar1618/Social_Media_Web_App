#all followed steps are written here for other projects reference and for practice and understanding the flow of project

step 1 : create a react project
step 2 : install all the depnedencies
//list of dependencies required for the project

step 3: create a .env file
//list .env variables

--
how to access .env variables in vite
import.mega.env.VITE_APPWRITE_MY_VARIABLE

step 4 : go to app write create your project
copy all the project ids and api key to .env file
add .env file to .gitignore
